# Regression Test Ownership & Location

Current status: UNASSIGNED.

Expected location: `tests/` directory or a `tests` entry in `docs/MANIFEST.json` (e.g., `"tests": {"command": "pytest", "path": "tests/", "owner": "team-or-person"}`).

Action: Update `docs/MANIFEST.json` with a `tests` entry when an owner and command are allocated.